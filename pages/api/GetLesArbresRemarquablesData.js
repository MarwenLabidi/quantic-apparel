
//NOTE : use start query parameter to get the next page of data (pagination) 10 items per page
export default function handler(req, res) {
  res.status(200).json({ name: 'GetLesArbresRemarquablesData' })
}
